export const environment = {
    production: false,
    fyle_url: $ENV.FYLE_URL,
    fyle_app_url: $ENV.FYLE_URL,
    fyle_client_id: $ENV.FYLE_CLIENT_ID,
    callback_uri: $ENV.CALLBACK_URI,
    api_url: $ENV.API_URL,
    app_url: $ENV.APP_URL,
    sentry_dsn: $ENV.SENTRY_DSN,
    release: $ENV.RELEASE
};
